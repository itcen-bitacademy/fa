<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu48">
	<!-- Team -->
	<insert id="save" parameterType="test4vo">
		<![CDATA[
		insert
		  into test
		values (null, #{name })
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	<!-- insert -->
	<insert id="insert" parameterType="ldebtvo">
		<![CDATA[
		insert
		  into tb_l_term_debt
		values (null, #{code},#{name},#{majorCode},#{debtAmount}, 0, #{repayWay}, #{debtDate}, #{expDate}, #{intRate}, #{intPayWay}, #{intAmount}, #{mgr}, #{mgrCall}, #{bankCode},#{depositNo},'2401101',null,'N','u009',now(),null,null)
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	<!-- select list -->
	<select id="selectAll" resultType="ldebtvo">
	<![CDATA[
		select no,
			code,
   			name,
    		major_code as majorCode,
    		debt_amount as debtAmount,
    		repay_bal as repayBal,
    		repay_way as repayWay,
    		concat(date_format(dept_date,'%Y/%m/%d'),'-',
    		date_format(exp_date,'%Y/%m/%d')) as debtExpDate,
    		int_rate as intRate,
    		int_pay_way as intPayWay,
   			mgr,
    		mgr_call as mgrCall,
		    bank_code as bankCode,
		    deposit_no as depositNo
		    from tb_l_term_debt
	]]>
	</select>
	
	<!-- update -->
	<update id="update" parameterType="ldebtvo">
	<![CDATA[
		update tb_l_term_debt 
		set name = #{name},
			major_code = #{majorCode},
			debt_amount = #{debtAmount},
			repay_way=#{repayWay},
			dept_date=#{debtDate},
			exp_date=#{expDate},
			int_rate=#{intRate},
			int_pay_way=#{intPayWay},
			int_amount=#{intAmount},
			mgr=#{mgr},
			mgr_call=#{mgrCall},
			bank_code=#{bankCode},
			deposit_no=#{depositNo},
			update_id='u009',
			update_date=now()
		where no=#{no}
	
	]]>
	</update>
	<!-- delete -->
	<update id="delete" parameterType="long">
	<![CDATA[
		update tb_l_term_debt 
		set delete_flag='Y'
		where no=#{no}
	
	]]>
	</update>
</mapper>