<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu48">
	<!-- Team -->
	<insert id="save" parameterType="test4vo">
		<![CDATA[
		insert
		  into test
		values (null, #{name })
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	<!-- insert -->
	<insert id="insert" parameterType="ldebtvo">
		<![CDATA[
		insert
		  into tb_l_term_debt
		values (null, #{code},#{name},#{majorCode},#{debtAmount}, 0, #{repayWay}, 'N',#{debtDate}, #{expDate}, #{intRate}, #{intPayWay}, #{intAmount}, #{mgr}, #{mgrCall}, #{bankCode},#{depositNo},'2401101',null,'N',#{insertId},now(),null,null)
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	
	<select id="selectAllCount" resultType="int" parameterType="map">
	<![CDATA[
		select count(*)
		    from tb_l_term_debt
		    where delete_flag= "N"
	]]>
	<if test="year.length()>0 ">
	<![CDATA[
	and 	date_format(debt_date,'%Y') like concat('%',#{year},'%') 
	]]>
	</if>
	<if test="code.length()>0 ">
	<![CDATA[
	and 	code like concat('%',#{code},'%') 
	]]>
	</if>
	</select>
	
	<!-- select list -->
	<select id="selectAll" resultType="ldebtvo" parameterType="map">
	<![CDATA[
		select no,
			code,
   			name,
    		major_code as majorCode,
    		debt_amount as debtAmount,
    		repay_bal as repayBal,
    		repay_way as repayWay,
    		concat(date_format(debt_date,'%Y/%m/%d'),'-',
    		date_format(exp_date,'%Y/%m/%d')) as debtExpDate,
    		int_rate as intRate,
    		int_pay_way as intPayWay,
   			mgr,
    		mgr_call as mgrCall,
		    bank_code as bankCode,
		    deposit_no as depositNo
		    from tb_l_term_debt
		    where delete_flag= "N"
	]]>
	<if test="year.length()>0 ">
	<![CDATA[
	and 	date_format(debt_date,'%Y') like concat('%',#{year},'%') 
	]]>
	</if>
	<if test="code.length()>0 ">
	<![CDATA[
	and 	code like concat('%',#{code},'%') 
	]]>
	</if>
	<if test="pagination.pageIndex > 0">
            <![CDATA[
                limit    #{pagination.pageIndex }, #{pagination.listSize }
             ]]>
    </if>
	</select>
	
	
	<select id="selectsection" resultType="sectionvo">
	<![CDATA[
		select no,
			classification,
			code,
			last_update as lastUpdate,
			parent_no as parentNo
		    from tb_section
		    where parent_no= 6
	]]>
	
	</select>
	
	
	<!-- update -->
	<update id="update" parameterType="ldebtvo">
	<![CDATA[
		update tb_l_term_debt 
		set name = #{name},
			major_code = #{majorCode},
			debt_amount = #{debtAmount},
			repay_way=#{repayWay},
			debt_date=#{debtDate},
			exp_date=#{expDate},
			int_rate=#{intRate},
			int_pay_way=#{intPayWay},
			int_amount=#{intAmount},
			mgr=#{mgr},
			mgr_call=#{mgrCall},
			bank_code=#{bankCode},
			deposit_no=#{depositNo},
			update_id=#{updateId},
			update_date=now()
		where no=#{no}
	
	]]>
	
	</update>
	<!-- delete -->
	<update id="delete" parameterType="list">
	<![CDATA[
		update tb_l_term_debt 
		set delete_flag='Y'
		where no in 
	]]>
	<foreach item="item" index="index" collection="list" close=")" open="(" separator=",">
		#{item }
	</foreach>
	
	</update>
</mapper>