<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu25">
	<!-- Test -->
	<insert id="create" parameterType="bankaccountvo">
		<![CDATA[
		insert into 
		tb_bank_account 
		values(#{depositNo}, #{depositHost}, #{bankCode}, #{makeDate}, 
			   #{balance}, #{enDate}, #{depositLimit}, #{profit}, #{banker},
			   #{accountNo}, #{insertUserId}, #{insertDay}, 
			   #{updateUserId}, #{updateDay}, #{deleteFlag});
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	
	<update id="update" parameterType="bankaccountvo">
		<![CDATA[
		update tb_bank_account
		set deposit_no = #{depositNo},
			deposit_host = #{depositHost}, 
			bank_code = #{bankCode},
			makedate = #{makeDate},
			balance =  #{balance},
			endate = #{enDate},
			deposit_limit = #{depositLimit},
			profit = #{profit},
			banker = #{banker},
			account_no = #{accountNo},
			update_userid = #{updateUserId},
			update_day = now()
		where deposit_no = #{depositOld};
		]]>
	</update>
	<delete id="delete" parameterType="bankaccountvo">
		<![CDATA[
		delete from tb_bank_account
		where deposit_no = #{depositNo};
		]]>
	</delete>

</mapper>