<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu20">

	<select id="selectClosingYear" resultType="string">
		select	substr(cd.closing_year_month, 1, 4) as closing_year
		  from	tb_closing_date as cd
		 group	by closing_year
		 order	by closing_year desc
		;
	</select>
	
	<select id="selectClosingDateByYear" resultType="closingDate17Vo" parameterType="string">
		select	
			no
		    , closing_year_month as closingYearMonth
		    , closing_date
		    , closing_statement_date
		    , closing_purchase_date
		    , closing_sales_date
		    , closing_assets_date
		    , closing_debt_date
		    , closing_settlement_date
		    , closing_yn
		    , use_yn
		    , insert_userid
		    , insert_day
		    , update_userid
		    , update_day
		  from	tb_closing_date
		 where	use_yn = true
		   and	substr(closing_year_month, 1, 4) = #{year }
		 order	by closing_year_month desc
		;
	</select>
	
</mapper>