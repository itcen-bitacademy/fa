<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu19">

	<!--  TEST -->
   <insert id="save" parameterType="test19vo">
      <![CDATA[
      insert
        into test
      values (null, #{name })
      ]]>
      <selectKey keyProperty="no" resultType="long" order="AFTER">
         <![CDATA[
         select last_insert_id()
         ]]>
      </selectKey>
   </insert>


	<insert id="insertClosingDate" parameterType="closingDate19Vo">
		insert	into tb_closing_date (
			closing_year_month
		    , closing_date
		    , closing_statement_date
		    , closing_purchase_date
		    , closing_sales_date
		    , closing_assets_date
		    , closing_debt_date
		    , closing_settlement_date
		    , insert_userid
		) values (
			#{closingYearMonth }
			, #{closingDate }
		    , #{closingStatementDate }
		    , #{closingPurchaseDate }
		    , #{closingSalesDate }
		    , #{closingAssetsDate }
		    , #{closingDebtDate }
		    , #{closingSettlementDate }
		    , #{insertUserid }
		);
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>
	
	<select id="selectAllClosingDate" resultType="closingDate19Vo">
		select	
			no
		    , closing_year_month as closingYearMonth
		    , closing_date
		    , closing_statement_date
		    , closing_purchase_date
		    , closing_sales_date
		    , closing_assets_date
		    , closing_debt_date
		    , closing_settlement_date
		    , closing_yn
		    , use_yn
		    , insert_userid
		    , insert_day
		    , update_userid
		    , update_day
		  from	tb_closing_date
		 where	use_yn = true
		 order	by closing_year_month desc
		;
	</select>
	
	<update id="updateClosingDate" parameterType="closingDate19Vo">
		update	tb_closing_date
		   set	closing_year_month = #{closingYearMonth }
				, closing_date = #{closingDate }
		        , closing_statement_date = #{closingStatementDate }
		        , closing_purchase_date = #{closingPurchaseDate }
		        , closing_sales_date = #{closingSalesDate }
		        , closing_assets_date = #{closingAssetsDate }
		        , closing_debt_date = #{closingDebtDate }
		        , closing_settlement_date = #{closingSettlementDate }
		        , update_userid = #{updateUserid}
		        , update_day = now()
		 where	no = #{no }
		   and	closing_yn = false
		;
	</update>
	
	<delete id="deleteClosingDate" parameterType="closingDate19Vo">
		delete	from tb_closing_date
		 where	no = #{no }
		   and	closing_yn = false
		;
	</delete>
</mapper>