<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="menu38">
	<!-- Team -->
	<insert id="save" parameterType="test2vo">
		<![CDATA[
		
		]]>
		<![CDATA[
		insert
		  into test
		values (null, #{name })
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			<![CDATA[
			select last_insert_id()
			]]>
		</selectKey>
	</insert>
	<!-- 세금계산서의 모든 리스트를 가져오는 select -->
	<select id="getAllBuyTaxbill" resultType="buytaxbillvo">
		<![CDATA[
		select *
		  from tb_buy_taxbill
		]]>
	</select>
	<!-- 세금계산서의 맞는 거래처List를 가져오는 select -->
	<select id="getMatchTaxbillCustomerList" resultType="purchasecustomer35vo">
		<![CDATA[
		select *
		  from tb_purchasecustomer
		]]>
	</select>
	<!-- 세금계산서에 맞는 품목을 가져오는 select -->
	<select id="getMatchTaxbillItemsList" resultType="buytaxbillitems">
		<![CDATA[
		select *
		  from tb_buy_taxbill_items
		]]>
	</select>
</mapper>